<h1>let's see your tasks</h1>

<ul>
<% @tasks.each do |task| %>

<li>
<%= link_to task.content, task_path(task)%> <%= link_to 'edit', edit_task_path(task) %> <%= link_to 'delete', task_path(task),
  method: :delete,
  data:{confirm: 'are you sure?'} %>
</li>

<%end%>

</ul>

<h3><%= link_to 'here if you want to create a new task', new_task_path %></h3>